# ============================================================
#  GitHubに上げてはいけないものリスト（git が記録しなくなる）
#  ※ ここに書いたファイルは、自分のパソコンには残ります
# ============================================================

# --- パスワードなどの秘密情報（includes/ に引っ越し済み） ---
includes/mail_config.php

# --- DB接続情報（本番のパスワード等が入るため除外。見本は includes/db_config.php.example） ---
includes/db_config.php

# --- 登録された個人情報の本体（名前・メール・パスワード） ---
data/*.csv

# --- バックアップファイル（backup/ フォルダにまとめて置く） ---
*.bak
backup/

# --- テスト用会員のログイン情報（メール・パスワードが平文のため除外） ---
includes/db_setup_testusers.sql

# --- エラーログ（サーバーのパス情報が含まれるゴミファイル） ---
debug.log

# --- さくらの接続先情報を含む個人メモ（手元には残す／公開しない） ---
tasks/learn_sakura_db.md

# --- Mac が勝手に作るゴミファイル ---
.DS_Store
